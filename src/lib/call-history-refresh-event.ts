/** Dispatched by “Refresh list” and the live call dock when active lines change so the table refetches `/api/calls/inbound-history`. */
export const INBOUND_CALL_HISTORY_REFRESH_EVENT = "csicrm:refresh-inbound-call-history";
