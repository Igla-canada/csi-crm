/** Shared copy for RingCentral-imported call stubs (server + client). */
export const TELEPHONY_CALL_SUMMARY_PLACEHOLDER =
  "RingCentral call — complete this log (result, vehicle, quotes) when you review it.";
